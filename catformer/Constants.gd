class_name Constants

const PLAYER_SPEED = 300.0
const PLAYER_JUMP_VELOCITY = -550.0
const FALL_DEATH_Y = 800.0

const PLATFORM_SIZE = Vector2(128, 32)
const PLATFORM_VISUAL_SIZE = Vector2(64, 16)

const COLOR_PLAYER = Color(0.8, 0.4, 0.8, 1)
const COLOR_PLATFORM = Color(0.4, 0.8, 0.4, 1)
const COLOR_GROUND = Color(0.6, 0.4, 0.2, 1)
const COLOR_GOAL = Color(1, 0.8, 0, 1)

const LEVEL_LABEL_FONT_SIZE = 24